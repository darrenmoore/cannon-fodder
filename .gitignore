node_modules/
game/public/bundle.js
game/public/bundle.js.map

# Screenshot output from tools/shoot.mjs and tools/playtest.mjs.
game/shots/

# Personal Claude Code settings -- RENDER_URL, RENDER_API_KEY and the like.
# .claude/settings.json and .claude/skills/ ARE tracked; this one is not.
.claude/settings.local.json

# The sprite gallery, emitted into public/ only by `npm run dev` (build.mjs).
# Dev-only means absent: a production build deletes these rather than hiding them.
game/public/sprites.js
game/public/sprites.js.map
game/public/sprites.html

# The frame bench's copy of the reference sheet: 2.8MB of base64 regenerated in
# one command, and the PNG it comes from is already tracked beside it. The bench
# itself (temp/trumper-frames.html) IS tracked -- it is what settled which cells
# Major Trumper animates on, and cut-sheet.mjs cites it.
temp/sheet.js
